set(PYLADA_HEADERS debug.hpp misc.hpp numpy_types.h std_map.hpp std_map.impl.hpp std_vector.hpp xml.hpp)
add_library(pylada ${PYLADA_HEADERS})
target_link_libraries(pylada ${Boost_PYTHON_LIBRARY} crystal opt math)
if(MPI_FOUND)
  include_directories(${MPI_INCLUDE_PATH})
  target_link_libraries(pylada mpi)
endif(MPI_FOUND)
if(vff_functional)
  target_link_libraries(pylada vff)
endif(vff_functional)
if(ce_functional)
  target_link_libraries(pylada ce)
endif(ce_functional)
if(atompot_functional)
  target_link_libraries(pylada atompot)
endif(atompot_functional)
if(pcm_functional)
  target_link_libraries(pylada pcm)
endif(pcm_functional)
if(enum_package)
  target_link_libraries(pylada enum)
endif(enum_package)
if(minimizer_libs)
  target_link_libraries(pylada minimizer)
endif(minimizer_libs)

if(NOT noinstall_headers)
  install(FILES ${PYLADA_HEADERS} DESTINATION include/LaDa/python)
endif(NOT noinstall_headers)
