add_library(pylada debug.hpp misc.hpp numpy_types.h std_map.hpp std_map.impl.hpp std_vector.hpp xml.hpp)
target_link_libraries(pylada ${Boost_PYTHON_LIBRARY} crystal opt math)
if(MPI_FOUND)
  include_directories(${MPI_INCLUDE_PATH})
  target_link_libraries(pylada mpi)
endif(MPI_FOUND)
if(vff_functional)
  target_link_libraries(pylada vff)
endif(vff_functional)
if(ce_functional)
  target_link_libraries(pylada ce)
endif(ce_functional)
